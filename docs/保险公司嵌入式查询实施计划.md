# 保险公司嵌入式查询实施计划

> 日期：2026-08-14  
> 开发分支：`feature/insurance-embed-query`  
> 执行规则：一次只推进一个步骤，用户确认后再进入下一步。

## 实施步骤

- [x] 1. 建立独立页面源码目录
  - 已创建 `insurance-embed/index.html` 和 `insurance-embed/README.md`。
  - 已验证仓库基线与原始 `index(1).html` 的 SHA-256 一致。
  - 未修改原始微信附件，未应用功能或视觉改动。

- [x] 2. 编写正式设计文档和接口清单
  - 设计文档：`docs/保险公司嵌入式查询页面设计.md`。
  - 接口清单：`docs/保险公司嵌入式查询接口清单.md`。
  - 固化页面、状态、鉴权、批量、取消、导出和计费约定。
  - 已核对现有 `/company/embed/medical/query-types`、`query`、`usage` 接口并确定兼容策略。

- [ ] 3. 检查并设计数据库变更
  - 盘点现有查询日志、历史数据、额度和价格表。
  - 设计批次、请求明细、动态结果和查看状态结构。

- [ ] 4. 编写数据库迁移脚本
  - 新增必要表、索引和状态字段。
  - 验证迁移脚本和现有数据兼容性。

- [ ] 5. 统一保险公司 AppKey 鉴权
  - 统一 `X-App-Key` 解析、公司状态校验和公司上下文。
  - 配置独立部署所需 CORS 白名单。

- [ ] 6. 实现查询项目及 AppKey 验证接口
  - 实现真实接入验证。
  - 规范查询项目、公司价格和用量响应。

- [ ] 7. 实现单人实时查询后端链路
  - 复用数据源适配层、日志、预算预留和计费确认。

- [ ] 8. 实现单人精准延时提交链路
  - 实现提交、重复复用、状态、详情和结果可见性。

- [ ] 9. 实现精准延时管理后台
  - 实现开始处理、动态结果导入/编辑、草稿和上传完毕。

- [ ] 10. 实现批量名单解析与校验接口
  - 支持 `.xlsx/.xls`、500 人上限和可编辑预览。

- [ ] 11. 实现批次创建和人员提交
  - 服务端二次校验并事务创建批次及明细。

- [ ] 12. 实现批量实时任务调度
  - 每个批次最多 5 条并发，完成一条立即补充下一条。

- [ ] 13. 实现批量精准延时处理
  - 创建或复用人员请求，汇总批次进度。

- [ ] 14. 实现取消功能
  - 支持取消可取消的整批或单人记录。

- [ ] 15. 实现历史记录与新结果提醒
  - 实现统一历史、详情、未读提醒和已查看状态。

- [ ] 16. 实现 Excel 结果导出
  - 支持单人和终态整批 `.xlsx` 导出。

- [ ] 17. 将独立页面改成亮色布局
  - 完成查询工作台、历史、提醒和响应式布局。

- [ ] 18. 接入 AppKey 和单人查询页面
  - 删除前端模拟逻辑，接入单人实时和精准延时接口。

- [ ] 19. 完成批量导入可编辑界面
  - 实现上传、修改、新增、删除、恢复、校验和提交。

- [ ] 20. 完成批次、历史、提醒、取消和导出界面
  - 接入全部保险公司端操作。

- [ ] 21. 执行测试和安全检查
  - 覆盖公司隔离、500 人、5 并发、状态竞争、计费和 Excel。

- [ ] 22. 独立部署与最终验收
  - 验证 HTTPS、CORS、浏览器、iframe 和 WebView。

## 当前状态

第 2 步已完成，当前等待用户确认。用户明确开始下一步后，才进入第 3 步数据库变更设计。
