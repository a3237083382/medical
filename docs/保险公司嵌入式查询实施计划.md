# 保险公司嵌入式查询实施计划

> 日期：2026-08-14  
> 开发分支：`feature/insurance-embed-query`  
> 执行规则：一次只推进一个步骤，用户确认后再进入下一步。

> 2026-08-17 优先级调整：今天优先完成精准延时（非实时）端到端闭环。实时批量调度、每批最多 5 条并发和数产实时接口对接保留需求，但延后到非实时闭环完成之后。

## 实施步骤

- [x] 1. 建立独立页面源码目录
  - 已创建 `insurance-embed/index.html` 和 `insurance-embed/README.md`。
  - 已验证仓库基线与原始 `index(1).html` 的 SHA-256 一致。
  - 未修改原始微信附件，未应用功能或视觉改动。

- [x] 2. 编写正式设计文档和接口清单
  - 设计文档：`docs/保险公司嵌入式查询页面设计.md`。
  - 接口清单：`docs/保险公司嵌入式查询接口清单.md`。
  - 固化页面、状态、鉴权、批量、取消、导出和计费约定。
  - 已核对现有 `/company/embed/medical/query-types`、`query`、`usage` 接口并确定兼容策略。

- [x] 3. 检查并设计数据库变更
  - 盘点现有查询日志、历史数据、额度和价格表。
  - 设计批次、请求明细、动态结果和查看状态结构。
  - 已只读核对本机 `ry_vue` 实际表结构。
  - 数据库设计：`docs/保险公司嵌入式查询数据库设计.md`。

- [x] 4. 编写数据库迁移脚本
  - 新增必要表、索引和状态字段。
  - 验证迁移脚本和现有数据兼容性。
  - 迁移脚本：`RuoYi-Vue/sql/biz_migration_08_embedded_query_workflow.sql`。
  - 已完成静态检查：包含 4 张新表、6 个扩展字段、20 个幂等索引检查和停用的精准延时价格占位配置。
  - 未连接数据库执行迁移，未修改本机 `ry_vue` 数据或结构。

- [x] 5. 统一保险公司 AppKey 鉴权
  - 统一 `X-App-Key` 解析、公司状态校验和公司上下文。
  - 配置独立部署所需 CORS 白名单。
  - `/company/embed/medical/**` 已统一经过 AppKey 拦截器，预检请求除外。
  - 无效 AppKey 返回 `401 INVALID_APP_KEY`，停用公司返回 `403 COMPANY_DISABLED`。
  - 嵌入接口的允许来源通过 `EMBEDDED_CORS_ALLOWED_ORIGIN_PATTERNS` 配置，默认仅允许本机开发来源。
  - 已执行完整后端测试：28 个测试全部通过，其中新增鉴权和 CORS 测试 8 个。

- [x] 6. 实现查询项目及 AppKey 验证接口
  - 实现真实接入验证。
  - 规范查询项目、公司价格和用量响应。
  - 已新增 `GET /company/embed/medical/access`，返回脱敏公司信息、服务状态和能力开关。
  - 查询项目已按全局状态和公司级状态过滤，并返回公司查得价格；未查得价格固定为 `0.00`。
  - 用量接口已统一预算开关及 `NORMAL/NEAR_LIMIT/LIMIT_REACHED` 状态。
  - 已执行完整后端测试：33 个测试全部通过，其中基础信息控制器测试 5 个。

- [x] 7. 实现单人实时查询后端链路
  - 复用数据源适配层，并将外部数据源调用放在事务之外；请求创建与额度预留、结果落库与计费确认分别使用短事务。
  - 实时请求写入统一请求表和动态结果表，成功、未查得及失败均写入不可修改的脱敏查询日志。
  - `HIT` 按公司价格计费，`NO_RESULT` 和 `FAILED` 固定为 `0.00`；失败时释放已预留额度。
  - 对外返回请求编号、处理/上传/结果状态、实际费用和脱敏后的动态结果；数据源异常返回稳定错误码 `SOURCE_UNAVAILABLE`。
  - 已执行完整后端测试：36 个测试全部通过，其中单人实时服务测试 7 个、嵌入控制器测试 6 个。
  - 第 4 步迁移脚本仍未执行，因此本步骤未启动应用或连接数据库做实时查询联调。

- [x] 8. 实现单人精准延时提交链路
  - 已实现 `POST /company/embed/medical/delayed/requests`，创建 `PENDING + NOT_UPLOADED` 的单人精准延时请求并预留查得费用。
  - 提交事务先锁定当前公司，再检查同公司、同姓名和身份证号的未完成请求；命中时返回原请求及 `reused=true`，不重复创建或预留额度。
  - 已实现 `GET /company/embed/medical/requests/{requestNo}`，详情始终按当前 AppKey 对应 `company_id` 隔离。
  - `NOT_UPLOADED` 只返回处理状态，不读取或返回后台草稿；`UPLOADED` 才返回脱敏后的动态字段和结果数据。
  - 已校验姓名及 18 位身份证格式，停用服务、额度不足和请求不存在均返回稳定错误码。
  - 已执行完整后端测试：44 个测试全部通过，其中新增延时服务测试 6 个，嵌入控制器测试增至 8 个。
  - 第 4 步迁移脚本仍未执行，因此本步骤未连接数据库进行延时请求联调。

- [x] 9. 实现精准延时管理后台
  - 已实现精准延时请求列表、详情、原子开始处理、动态结果 Excel 预览、草稿保存、上传完毕和已上传内容修正接口。
  - 上传完毕事务统一写结果、确认或释放预留额度、写不可修改查询日志并将请求更新为 `COMPLETED + UPLOADED + UNREAD`。
  - `HIT` 必须包含结果数据并按预留查得价格收费；`NO_RESULT`、`HINT_ONLY` 固定为 `0.00`，其中 `HINT_ONLY` 必须填写提示说明。
  - 已上传结果只允许修改动态内容和说明并递增结果版本，不允许改变已经固化的结果状态和计费凭证，修改时必须填写修改说明。
  - 已新增若依管理页面 `business/delayed-query/index`，支持筛选、开始处理、Excel 导入、动态列/行编辑、保存草稿、上传和修正结果。
  - 迁移脚本已补充 6 个幂等菜单及权限节点，但仍未连接数据库执行。
  - 已执行完整后端测试：50 个测试全部通过，其中新增精准延时管理服务测试 6 个；管理前端生产构建通过。

- [x] 10. 调整计划并冻结非实时优先范围
  - 今天只推进精准延时单人、批量、管理处理、历史提醒、取消、导出和独立页面接入。
  - 暂缓实时批量任务调度、每批最多 5 条并发和数产实时数据源对接。
  - 保留已经完成的单人实时后端代码，不在本轮删除或重构。

- [x] 11. 执行数据库迁移并验证菜单
  - 执行 `biz_migration_08_embedded_query_workflow.sql`，创建工作流表、扩展字段、索引、精准延时价格占位和管理菜单。
  - 核对当前数据库、备份条件、表结构、菜单权限和应用启动结果。
  - 本步骤会修改数据库，开始执行前必须再次获得用户明确同意。
  - 已备份受影响的现有表至 `D:\medical-backups\ry_vue_before_embedded_query_20260817_123540.sql`。
  - 已在本机 MySQL 8.0.46 的 `ry_vue` 执行迁移并核对：4 张工作流表、6 个扩展字段、全部索引、停用的精准延时价格占位和 6 个管理菜单节点均已创建。
  - 后端已在 `8088` 端口运行，`GET /captchaImage` 返回 HTTP 200；默认管理员可直接看到新增菜单，普通角色仍需按实际职责分配菜单权限。

- [x] 12. 实现精准延时批量名单解析与校验
  - 支持 `.xlsx/.xls`、最多 500 人、18 位身份证格式、空值和名单内重复校验。
  - 返回可编辑预览，支持修改后重新校验；存在错误时禁止正式提交。
  - 已实现 `POST /company/embed/medical/batches/import-preview` 和 `POST /company/embed/medical/batches/validate`，两个接口均只返回预览，不创建批次、请求或计费记录。
  - Excel 必须包含姓名和身份证号列，支持中英文表头别名、10 MB 文件上限、空行忽略、原始值保留、当前值规范化以及重复组全员标错。
  - 已执行完整后端测试：58 个测试全部通过，其中新增批量解析与校验测试 6 个；新版后端已在 `8088` 启动，真实 AppKey 冒烟测试通过且请求表记录数未变化。

- [x] 13. 实现精准延时批次提交与请求复用
  - 事务创建批次和成员；同公司、同姓名和身份证号存在未完成请求时复用。
  - 复用请求不重复预留额度或收费，批次成员保存原始行顺序和复用标记。
  - 已实现 `POST /company/embed/medical/batches`，当前非实时优先阶段只接受 `serviceMode=DELAYED`，实时批次仍保持延后。
  - 服务端在事务前重新校验名单，在事务内锁定公司、识别全部复用请求、汇总新增请求预留额并一次校验额度，再创建批次、请求和成员关联。
  - 批次成员保存页面行号、请求 ID、复用标记和活动状态；复用请求不依赖当前价格配置，也不新增请求或重复预留额度。
  - 已执行完整后端测试：64 个测试全部通过，其中新增批次提交服务测试 5 个；新版后端已在 `8088` 启动，不落库接口冒烟测试通过。

- [x] 14. 实现精准延时取消与批次进度
  - 支持取消整个批次或单个人员。
  - 只有 `PENDING + NOT_UPLOADED` 可取消；取消后释放预留额度、保留状态记录且不收费。
  - 汇总批次待处理、处理中、完成、失败、取消和费用状态。
  - 已实现批次进度、整批取消和按成员 ID 单人取消接口，全部按当前 AppKey 公司隔离。
  - 复用成员只取消当前批次关联；非复用请求没有其他活动引用时才取消底层请求并释放月度预留额度，允许相同人员重新提交。
  - 整批取消会跳过处理中或已完成成员并返回不可取消数量；取消不写查询计费日志，不保存取消人、取消时间或原因。
  - 进度从成员和请求状态实时汇总并回写批次快照，覆盖待处理、处理中、完成、查得、未查得、失败、取消、可取消数量和实际费用。
  - 已执行完整后端测试：70 个测试全部通过，其中新增取消服务测试 5 个；真实数据库汇总 SQL 冒烟测试返回预期错误码。

- [x] 15. 完善精准延时管理后台批次处理
  - 增加批次筛选、批次详情和人员明细入口。
  - 管理人员可从批次或单人入口开始处理、保存草稿和上传结果。
  - 已新增管理端批次列表和批次详情接口，批次状态、进度、结果数量和费用根据成员请求实时汇总。
  - 管理页面已增加“人员请求 / 批次处理”页签，支持按批次编号、公司、状态和提交时间筛选。
  - 批次详情展示原名单顺序、复用标记、人员状态、结果和费用，成员可直接开始处理或进入原有结果编辑器。
  - 未新增数据库结构，批次入口复用现有原子开始处理、草稿保存、结果上传和已上传修正逻辑。
  - 已执行完整后端测试：74 个测试全部通过；管理前端生产构建通过；真实 MySQL 批次汇总和成员查询 SQL 验证通过且未写入测试数据。

- [x] 16. 实现精准延时历史、提醒和查看状态
  - 提供单人和批次历史、请求详情、未读结果提醒及标记已查看接口。
  - 所有数据按当前 AppKey 对应公司隔离，未上传草稿不可见。
  - 已新增单人历史、批次历史、未读数量和按请求编号标记已查看接口，最多返回最近 100 条历史记录。
  - 单人历史仅返回脱敏身份、状态、费用和时间，不批量返回任何动态结果；保险公司选择单人后才通过详情接口读取已上传结果。
  - 批次历史按成员当前状态实时汇总，避免依赖批次快照；未读只统计 `COMPLETED + UPLOADED + UNREAD` 请求。
  - 标记已查看只允许已完成且已上传的精准延时请求，重复调用保持幂等；全部查询和更新均带 `company_id` 条件。
  - 已执行完整后端测试：81 个测试全部通过；真实 AppKey 的三类只读接口返回 200，无效 AppKey 返回 `401 INVALID_APP_KEY`，冒烟过程未写入业务数据。

- [x] 17. 实现精准延时 Excel 导出
  - 支持单人结果 `.xlsx` 导出。
  - 支持终态整批结果导出，保留动态结果字段；存在待处理或处理中人员时禁止整批导出。
  - 单人导出按公司隔离；已上传结果导出动态字段，失败/取消等终态无动态结果时导出状态和说明，草稿不导出。
  - 已新增 `GET /company/embed/medical/requests/{requestNo}/export` 和 `GET /company/embed/medical/batches/{batchNo}/export`。
  - 整批导出包含 `批次汇总` 和各已上传人员的独立结果页，动态字段不同的人员不会互相覆盖。
  - 已执行完整后端测试：85 个测试全部通过；新增导出工作簿内容、公司隔离、草稿隔离和批次未完成拦截测试。

- [x] 18. 将独立页面改成亮色并接入单人精准延时
  - 删除前端模拟 AppKey、模拟延时和模拟结果逻辑。
  - 接入真实 AppKey 验证、单人精准延时提交、状态轮询、结果查看和管理后台处理回传链路。
  - 已将 `insurance-embed/index.html` 改为亮色响应式工作台，AppKey 仅保存在页面内存并通过 `/access` 验证。
  - 已接入单人精准延时提交、请求状态轮询和结果详情展示；删除默认 AppKey、mock 请求、演示场景及模拟结果。
  - 已完成嵌入页面 JavaScript 语法检查、动态结果字段结构核对及 `git diff --check`；未执行第 19 步。
  - 已根据宽屏验收截图将桌面工作区调整为全宽、全高布局，仅保留紧凑页面边距；窄屏继续使用上下流式布局。
  - 已新增独立 `config.js` 运行时 API 地址配置，本地页面从 `63342` 预览时请求后端 `8088`，避免误请求静态文件服务；已验证 `8088` 返回 JSON 鉴权响应并允许 `localhost:63342` 跨域。
  - 已完成 AppKey 会话保留：刷新页面自动恢复已验证 AppKey，关闭页面后通过 `sessionStorage` 清除；已完成后台上传结果后的 5 秒自动同步，页面隐藏时暂停、重新显示时立即同步。

- [ ] 19. 接入精准延时批量、历史、提醒、取消和导出界面
  - 完成名单上传、可编辑预览、重新校验、批次提交和进度展示。
  - 完成历史记录、新结果提醒、单人/整批取消和两类 Excel 导出。

- [ ] 20. 执行精准延时端到端测试和安全检查
  - 覆盖公司隔离、重复复用、500 人限制、状态竞争、草稿不可见、取消、计费和 Excel。
  - 验证 `index.html -> 后端 -> 管理后台 -> 结果上传 -> 保险公司查看` 完整链路。

- [ ] 21. 完成精准延时独立部署与验收
  - 验证 HTTPS、CORS、浏览器、iframe、WebView 和 AppKey 不持久化。
  - 验收单人和批量精准延时流程，形成今天的非实时交付结果。

## 延后事项

- [ ] 实现批量实时任务调度，每个批次始终最多 5 条并发。
- [ ] 接入批量实时查询页面、批次进度和异常处理。
- [ ] 数产集团接口确定后替换实时 `MedicalDataSource`，保持保险公司接口不变。
- [ ] 对实时批量链路执行专项测试和最终验收。

## 当前状态

第 18 步已完成，独立页面已改为亮色响应式工作台，保险公司可手工验证 AppKey、提交单人精准延时请求、轮询处理状态并在后台上传后查看脱敏结果。当前补充实现页面刷新保留 AppKey、关闭页面清除 AppKey，以及后台上传结果后的自动同步。

## 实时查询改造执行记录

- [x] 第 1 步：确认单人实时查询输入契约
  - 数产可信空间 SDK 示例 `C:\Users\mec\Desktop\java-client-demo825\src\main\java\com\hnchasing\digital\openapi\java\client\demo\springdemo\MisuanDemoTesthua.java` 已核对。
  - 外部请求参数固定为 `sfzhm`、`startdate`、`enddate`，时间格式为 `yyyy-MM-dd HH:mm:ss`。
  - 测试请求 `sfzhm=320683198312120713`、`startdate=2020-01-01 00:00:00`、`enddate=2026-01-01 00:00:00` 已成功返回数据。
  - 当前 `index.html`、后端实时接口和接口清单仍使用 `name`、`idCard` 旧参数，后续步骤需统一改为上述三字段；本步骤未修改业务代码。
- [x] 第 2 步：确认外部返回结果的记录边界
  - 已根据 SDK 成功调用返回结果和本地样例核对：返回体包含 `res` 数组，每个元素代表一次独立就诊记录。
  - 同一身份证在时间范围内可能对应多条记录，展示时必须逐次就诊保留，不能按身份证合并。
  - 记录按就诊类型区分为 `住院` 和 `门诊`：住院记录包含基本信息、电子病历和医学影像检查数据；门诊记录只展示基本信息表，病历和影像字段为空时不生成对应表。
  - 已记录外部字段为动态数据，具体字段名与中文列名映射放在下一步依据 `测试数据(1).xlsx` 和 `表结构(1).xlsx` 统一确定；本步骤未修改业务代码。
- [x] 第 3 步：完成 Excel 字段与三表输出设计
  - 已读取 `测试数据(1).xlsx` 和 `表结构(1).xlsx`，确认共 27 个原始字段及其中文列名。
  - 已确定每次就诊均生成基本信息表；住院记录追加电子病历表，存在影像字段时追加医学影像检查表；门诊记录不生成后两张表。
  - 已明确空值、日期格式、记录独立展示和身份证展示规则。
  - 设计文档：`docs/实时查询结果三表映射设计.md`；本步骤未修改业务代码。
- [x] 第 4 步：制定三表输出改造实施计划
  - 已明确页面、后端接口、SDK 数据源适配、三表组织、错误处理和验证标准。
  - 已确定按“后端请求与数据源 -> 结果映射 -> 页面展示 -> 文档 -> 联调”的顺序实施。
  - 实施计划：`docs/实时查询三表输出实施计划.md`；本步骤未修改业务代码。
- [x] 第 5 步：完成后端三字段请求转发基础改造
  - `DigitalIndustryDataSource` 对新契约请求直接转发 `sfzhm`、`startdate`、`enddate`，旧请求体格式继续兼容。
  - 实时查询服务已识别新三字段并校验缺失参数；历史/内部旧调用仍可使用姓名和身份证号。
  - 无结果判断已兼容外部 `res` 数组。
  - 新请求写入现有工作流时身份证号取 `sfzhm`，姓名缺省使用内部占位值，后续结果映射阶段再以外部返回姓名回填展示。
  - 已执行 `mvn -pl ruoyi-business -am test`：81 个测试全部通过。
- [x] 第 6 步：完成后端 `res` 三表结果组织
  - `MedicalQueryServiceImpl` 已将外部 `res` 数组逐条转换为 `visits`。
  - 每条就诊包含 `basicInfo`；住院记录按条件追加 `electronicMedicalRecord` 和 `medicalImaging`。
  - 27 个原始字段已转换为设计文档确定的中文列名，空值统一为空字符串。
  - 无影像字段的住院记录不生成空影像表，门诊记录不生成病历和影像表。
  - 已执行 `mvn -pl ruoyi-business -am test`：81 个测试全部通过。
- [x] 第 7 步：接入实时单人查询页面
  - `index.html` 实时模式已改为身份证号、开始时间、结束时间输入，姓名字段仅在精准延时模式显示。
  - 单人实时请求已发送 `sfzhm`、`startdate`、`enddate`，并校验身份证格式、时间必填和时间先后关系。
  - 页面已支持后端 `visits` 结构，按每次就诊渲染基本信息、电子病历和医学影像检查表。
  - 已完成内嵌 JavaScript 语法检查；实时批量 Excel 入口待后续批量实时链路步骤处理。
- [x] 第 8 步：同步实时查询接口清单
  - 单人实时查询示例已改为 `sfzhm`、`startdate`、`enddate`。
  - 已补充 `data.visits` 三表结果示例及门诊/住院返回规则。
  - 已注明实时批量接口仍未开放，避免文档与当前实现不一致。
- [x] 第 9 步：完成本轮静态与自动化验证
  - `mvn -pl ruoyi-admin -am test` 已通过：后端 32 个管理端测试全部通过，业务模块 81 个测试全部通过。
  - `index.html` 内嵌 JavaScript 语法检查通过，`git diff --check` 通过。
  - 已静态核对页面请求、后端转发和接口文档均使用 `sfzhm/startdate/enddate` 及 `visits` 结果结构。
  - 测试网关当前出现 DNS 解析异常，未完成本次外部真实数据联调；不影响本地编译和自动化测试结论。
- [x] 第 10 步：完成本地端到端冒烟检查
- 已确认后端 `8088` 端口和页面静态服务 `63343` 端口正在运行。
  - `GET /captchaImage` 返回 HTTP 200，后端可正常响应。
  - 未携带 AppKey 调用实时查询返回 HTTP 401，鉴权拦截符合预期。
  - 已使用新三字段请求体做接口格式冒烟；未使用真实 AppKey 发起扣费查询，避免写入测试业务数据。

- [x] 修正实时大数据入口旧参数校验并重启后端
  - `CompanyEmbedMedicalQueryController` 已将 `medical_all` 入口校验改为 `sfzhm/startdate/enddate`。
  - 已停止占用 jar 的旧进程，重新打包并启动新版后端；当前 `8088` 已监听。
  - 本次修正只影响实时大数据入口，精准延时仍保持姓名 + 身份证号。

### 待外部网络恢复后执行

- [ ] 使用有效 AppKey 在页面发起一次真实单人查询。
  - 前置条件：管理系统所在环境接入数产可信空间测试内网，测试网关 DNS 和网络可达。
  - 验证内容：请求参数、数产返回 `res`、住院/门诊三表展示、查询日志和费用状态。
  - 当前状态：顺延至明日内网可用后执行，今天不重复重试外部请求。

### 查询类型范围补充

- 当前实时三表输出仅适用于已接入的“大数据查询”接口（`medical_all`）。
- “医保查询”接口（`medical_insurance`）尚未与对方完成对接，不纳入本轮真实联调和结果验收。
- 后续医保接口接通后，需单独确认其请求参数、返回结构和展示规则，不直接复用大数据查询的三表映射结论。

### 大数据真实链路配置

- `medical_all` 已固定路由到 `digital` 数据源，关闭数据源不可用时回退模拟数据的行为。
- `DigitalIndustryDataSource` 已接入 `MisuanDemoTesthua.java` 同款 SDK 初始化和请求逻辑；测试凭据与网关配置已写入 `application.yml`，便于测试阶段直接修改。
- `ruoyi-admin` 已配置 Spring Boot `includeSystemScope`，数产 SDK 及其依赖已进入最终可运行 jar 的 `BOOT-INF/lib`。
- 已重新打包并启动管理端，日志出现 `Started RuoYiApplication`，8088 端口监听正常；`mvn -pl ruoyi-admin -am package -DskipTests` 构建成功。
- 真实查询仍需在数产可信空间内网可达且使用有效 AppKey 后执行，当前未虚报外部联调结果。
- 回归检查已完成：管理端 32 个测试、业务端 81 个测试全部通过；修正了实时查询测试用例，使其使用 `sfzhm/startdate/enddate` 新参数。
- 根据实际联调报错修复 Log4j 版本冲突：移除 SDK 携带的 `log4j-api-2.13.3`，最终 jar 统一使用 Spring Boot 管理的 Log4j 2.24.3；重新打包成功并通过 jar 内容检查。
- 内网对照联调已完成：直接运行 `MisuanDemoTesthua.java` 成功返回 4 条 `res` 记录；管理系统补齐 SDK 所需 `joda-time-2.9.9.jar`，重新打包启动成功，8088 端口正常监听。
- 根据真实返回字段修复结果展示：使用 `ryrq` 判断住院/门诊，住院记录固定输出基本信息、电子病历、医学影像检查三张表，门诊只输出基本信息表；同时对 SDK 返回的 GBK 乱码进行纠正。
- 进一步确认乱码来自 Windows JVM 默认编码：原始 demo 默认启动同样乱码，强制 UTF-8 后正常。管理端启动入口已固定 `file.encoding=UTF-8`，重新打包并启动成功。
