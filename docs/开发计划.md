# 湖南省医疗信息实时查询平台 — 开发计划

> 创建日期：2026-05-09（最后更新：2026-05-10）
> 开发方：湖南乐途科技有限公司
> 客户方：中国检验认证集团湖南分公司
> 最终用户：全省 51 家保险公司
> 数据来源：卫健委、卫生局、湖南数字产业集团
> 预计工期：约 40 个工作日（8 周）

---

## 技术栈

| 层面     | 选型                                          |
| -------- | --------------------------------------------- |
| 后端框架 | RuoYi-Vue (master, Spring Boot 4.x + JDK 17+) |
| 前端     | Vue3 + Element Plus + Vite                    |
| 接口层   | Magic-API 2.3.0 (org.ssssssss)                |
| 数据库   | MySQL 8.0 (utf8mb4)                           |
| 认证机制 | AppKey + HMAC-SHA256 签名                     |
| 缓存     | Redis                                         |

## 系统架构

```
管理后台 (RuoYi-Vue)                            对外接口 (Magic-API)
┌──────────────────────────────────┐           ┌──────────────────────────┐
│  管理员功能                        │           │ AppKey/Sign 认证拦截器    │
│  ┌────────────────────────────┐  │           │ 医疗数据查询接口          │
│  │ 保险公司管理(开户/充值/停用) │  │           │ 余额查询接口              │
│  │ 用户管理(RuoYi 内置)        │  │           │ 计费扣费引擎              │
│  │ 价目配置                   │  │           └──────────────────────────┘
│  │ 充值管理/扣费管理           │  │
│  │ 查询日志查看/导出           │  │                    保险公司自助门户
│  │ 费用流水查看/导出           │  │           ┌──────────────────────────┐
│  │ 统计分析仪表盘(ECharts)     │  │           │ 独立登录(与管理员分开)    │
│  │ 角色权限(RuoYi 内置)       │  │           │ 余额查询(含更新时间)      │
│  └────────────────────────────┘  │           │ 查询记录(只读)           │
│                                  │           │ 充值记录                 │
│  保险公司用户功能                  │           │ 修改密码/个人信息         │
│  ┌────────────────────────────┐  │           └──────────────────────────┘
│  │ 独立门户(Phase 4.11-4.17)  │  │
│  └────────────────────────────┘  │
└────────┬─────────────────────────┘
         │
         └──────────────┬───────────────────────┘
                        ▼
              ┌─────────────────────┐
              │  MySQL 数据库        │
              │  biz_insurance_company │
              │  biz_query_log      │
              │  biz_fee_flow       │
              │  magic_api_file     │
              │  sys_* (RuoYi 自带)  │
              └──────┬──────────────┘
                     │
                     ▼
         ┌───────────────────────────┐
         │  数据适配层 (Data Adapter) │
         │  对接真实数据源（Phase 7）  │
         └──────┬────────────────────┘
                │
    ┌───────────┼───────────────┐
    ▼           ▼               ▼
┌────────┐ ┌────────┐ ┌──────────────────┐
│ 卫健委  │ │ 卫生局  │ │ 湖南数字产业集团  │
│ 数据源  │ │ 数据源  │ │ 系统             │
└────────┘ └────────┘ └──────────────────┘
```

---

## Phase 1：项目脚手架搭建与环境验证（1-2 天）

**目标**：RuoYi-Vue 前后端在本地成功启动，admin 登录进入后台。

- [X] 1.1 修复本地 Java 环境（JAVA_HOME 指向 JDK 17）
- [X] 1.2 获取 RuoYi-Vue 源码（克隆或下载官方仓库）
- [X] 1.3 创建 MySQL 数据库 `ruoyi`，执行 RuoYi 初始化 SQL
- [X] 1.4 配置 `application-druid.yml` 数据库连接
- [X] 1.5 配置 `application.yml` 中的 Redis 连接
- [X] 1.6 启动后端，验证 `http://localhost:8080` 可用
- [X] 1.7 前端 `npm install` + `npm run dev` 启动
- [X] 1.8 验证 `admin/admin123` 能登录后台

**验证标准**：`admin/admin123` 成功登录，菜单正常显示。

---

## Phase 2：数据库业务表设计与创建（1 天）

**目标**：五张业务表创建完成，含索引、审计字段、触发器。计费模式为周期结算（先充值，周期内无限查询，周期结束统一扣费）。

### 表结构

| 表名                      | 说明           | 核心字段                                                                                                                            |
| ------------------------- | -------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| `biz_insurance_company` | 保险公司用户表 | id, company_name, app_key(唯一), app_secret, balance, balance_update_time, status；结算周期由若依 `sys_job` 调用目标参数统一控制 |
| `biz_query_price`       | 查询价目表     | id, query_type(唯一), query_name, fee, status                                                                                       |
| `biz_query_log`         | 查询日志表     | id, company_id, query_type, query_params(JSON), fee(待结算), settlement_id, status, request_time, request_ip                        |
| `biz_settlement`        | 结算记录表     | id, company_id, period_start, period_end, query_count, total_fee, balance_before, balance_after, status, settlement_time            |
| `biz_fee_flow`          | 费用流水表     | id, company_id, operation_type(RECHARGE/SETTLEMENT/REFUND), amount, balance_before, balance_after, operator, biz_id, operation_time |

- [X] 2.1 编写 `biz_insurance_company` 建表 SQL（含索引、审计字段）
- [X] 2.2 编写 `biz_query_price` 建表 SQL（含唯一索引 on query_type）
- [X] 2.3 编写 `biz_query_log` 建表 SQL（含禁止 UPDATE/DELETE 触发器）
- [X] 2.4 编写 `biz_settlement` 建表 SQL
- [X] 2.5 编写 `biz_fee_flow` 建表 SQL（含索引）
- [X] 2.6 合并 SQL 脚本并执行到数据库

**验证标准**：`DESC` 各表确认结构正确，触发器生效。

---

## Phase 3：Magic-API 集成（1 天）

**目标**：Magic-API 管理界面可访问，能编写并调用 SQL 接口。

- [X] 3.1 在 `ruoyi-admin/pom.xml` 添加 `magic-api-spring-boot-starter` 依赖
- [X] 3.2 配置 `application.yml`（database 存储模式、管理界面路径）
- [X] 3.3 在 `SecurityConfig.java` 中放行 `/magic/web/**` 和 `/magic/api/**`
- [X] 3.4 启动项目，进入 Magic-API Web IDE
- [X] 3.5 创建测试接口 `GET /magic/api/test/ping`，返回 `pong`

**验证标准**：访问 `http://localhost:8080/magic/web/` 进入 IDE，调用测试接口返回成功。

---

## Phase 4：保险公司管理模块（后台 CRUD，2 天）

**目标**：管理后台支持保险公司增删改查、AppKey/Secret 自动生成、状态管理、手动充值，以及查询接口价目管理。同时为保险公司提供独立登录门户，支持余额查询、日志查看等自助功能。

### 后台管理（管理员使用）

- [X] 4.1 用 RuoYi 代码生成器导入 `biz_insurance_company` 生成 CRUD
- [X] 4.2 将生成的代码解压到对应后端和前端目录
- [X] 4.3 实现 `AppKeyUtil.java`：生成 32 位 AppKey 和 64 位 AppSecret
- [X] 4.4 修改 Service：新增公司时默认不生成 AppKey/AppSecret，保险公司登录门户后自行新增/换发 AppKey
- [X] 4.5 配置菜单和权限标识
- [X] 4.6 实现启用/停用状态切换（Controller + 前端开关）
- [X] 4.7 充值流程改为：保险公司提交申请 → 管理员审核（见 Phase 4.19-4.22），原有管理员直接充值接口废弃
- [X] 4.8 前端适配：AppKey 复制按钮、余额格式化、充值对话框
- [X] 4.9 用代码生成器导入 `biz_query_price` 生成 CRUD（查询价目管理）
- [X] 4.10 查询价目管理：维护 query_type → fee 映射，每条记录对应一个查询接口的单价

### 保险公司自助门户（公司用户使用）

- [X] 4.11 `biz_insurance_company` 表新增字段：`username`(唯一), `password`, `login_ip`, `login_date`
- [X] 4.12 编写保险公司独立登录接口（JWT Token，与管理员登录隔离）
- [X] 4.13 创建保险公司登录页面（独立于 admin 登录页，路径 `/company/login`）
- [X] 4.14 实现保险公司首页/仪表盘：展示公司名称、当前余额、**余额更新时间**、**下次更新余额时间**（根据若依定时任务配置的统一结算周期计算）
- [X] 4.15 实现只读查询记录页面：保险公司只能看到自己公司的查询日志，不可删除（依赖 Phase 6 查询日志数据）
- [X] 4.16 实现提交充值申请页面：保险公司填写金额并提交，生成待审核的充值申请单
- [X] 4.17 实现充值记录页面：保险公司可查看所有已提交的充值申请单及审核状态（待审核/已通过/已驳回）
- [X] 4.18 实现修改密码/个人信息功能
- [X] 4.18.1 `/company/query` 调整为“接口接入”文档页，保险公司按接口文档在自有系统中调用开放 API，不在门户手工查询
- [X] 4.18.2 新增开放接口 `POST /open/api/medical/query`，支持 AppKey/AppSecret 签名、queryType 价目校验、查询日志记账；余额和费用流水由周期结算任务统一更新
- [X] 4.18.3 生产库执行 `biz_migration_03_company_self_appkey.sql`，允许新建保险公司时 `app_key/app_secret` 为空；验证信号：服务器新增公司插入语句回滚测试通过，现有 AppKey 查询接口列表返回 200，医保信息查询返回 3 条记录

### 嵌入式保险公司前端（Phase 4 增强）

- [X] 4.23 嵌入入口改为保险公司自助登录，不由管理员生成公司专属嵌入链接
- [X] 4.24 提供固定嵌入地址 `/company/login?embedMode=iframe&target=/company/query`
- [X] 4.25 保险公司在 iframe/WebView 内使用公司账号密码登录，登录后进入目标公司端页面
- [X] 4.26 嵌入模式只影响前端展示形态，不把 AppKey/API Key 或管理员 token 放入 URL
- [X] 4.27 登录成功后使用 `sessionStorage` 保存公司端 token 和必要公司信息
- [X] 4.28 保留 `/company/embed` 为兼容入口，访问时跳转到保险公司登录页，不再兑换 ticket
- [X] 4.29 嵌入模式隐藏退出按钮和系统标题，改为顶部标签导航
- [X] 4.30 管理后台移除“嵌入链接”操作，管理员只负责开户、停用、审核等后台职责
- [X] 4.31 企业端补齐费用流水只读页面，只查询当前公司数据
- [X] 4.32 新增本地 B/S iframe 测试页 `RuoYi-Vue3/public/company-iframe-test.html`
- [ ] 4.33 完成真实后端联调下的 iframe 登录成功链路浏览器测试（需 Redis/MySQL 后端运行）
- [ ] 4.34 完成 C/S WebView/CEF/Edge WebView2 现场行为验证（已在 `docs/phase4-验收记录.md` 记录验证方案）

### 管理员充值审核

- [X] 4.19 创建 `biz_recharge_request` 充值申请表（含字段：公司id、金额、状态[待审核/已通过/已驳回]、提交时间、审核时间、审核人、审核备注）
- [X] 4.20 实现管理员充值审核列表页面：展示所有待审核/已审核的充值申请，支持按公司名和状态筛选
- [X] 4.21 实现审核操作：管理员查看申请详情 → 通过（更新余额+记费用流水）或驳回（仅更新状态，不动余额）
- [X] 4.22 审核通过时：`@Transactional` 保证余额更新 + 费用流水 + 申请状态更新原子性

**验证标准**：
- 管理员后台：能创建公司（自动生成 AppKey/Secret），停用后状态标记正确
- 充值流程：保险公司提交申请 → 管理员审核通过 → 余额增加、费用流水记录；审核驳回 → 状态标记驳回、余额不变
- 保险公司门户：使用公司账号独立登录或嵌入式登录页进入，只能看到自己公司的余额、日志、费用流水和充值记录
- 接口接入页：展示 AppKey、开放接口地址、签名请求头、请求/响应示例和 queryType 价目，不展示 AppSecret，不提供手工医疗查询表单
- 开放接口：保险公司使用 `X-App-Key`、`X-Timestamp`、`X-Nonce`、`X-Sign` 调用 `/open/api/medical/query`，成功请求按价目写入查询日志；余额和 `SETTLEMENT` 费用流水由若依定时任务统一结算
- 嵌入式入口：固定地址进入公司登录页，登录后保持 iframe/WebView 模式，不触发管理员登录弹窗

---

## Phase 5：AppKey/Sign 签名认证系统（1.5-2 天）

**目标**：对外 API 路径经过签名认证，合法请求放行，非法请求拦截。

### 签名协议

```
请求头: X-App-Key, X-Timestamp, X-Nonce, X-Sign
签名算法: HMAC-SHA256(timestamp + "\n" + nonce + "\n" + body, appSecret)
时效窗口: ±5 分钟
Nonce 去重: Redis SET + EXPIRE (TTL = 6 分钟)
```

- [X] 5.1 实现 `SignUtil.java`（HMAC-SHA256 签名生成和验证）
- [X] 5.2 实现 `SignAuthInterceptor.java`（AppKey 查公司 → 校验状态 → 验证签名 → Nonce 去重）
- [X] 5.3 在 `WebMvcConfig.java` 注册拦截器到 `/api/v1/**`
- [X] 5.4 在 `SecurityConfig.java` 放行 `/api/v1/**`
- [X] 5.5 实现请求体缓存 Filter（复用 RuoYi `RepeatableFilter` 请求体缓存）
- [X] 5.6 创建受保护测试接口，并用单元测试验证签名流程
- [X] 5.7 编写客户端签名示例脚本（Python/Java/Shell）

**验证标准**：无签名头 → 401，签名错误 → 401，Nonce 重用 → 401，超时 → 401，正确签名 → 200。

---

## Phase 6：医疗数据查询 API + 计费引擎 + 数据适配层设计（核心业务，3-4 天）

**目标**：完整的"查询 → 查价 → 余额状态检查 → 脱敏 → 日志 → 周期结算扣费"链路跑通，同时设计数据适配层接口。

- [X] 6.1 设计并创建 `mock_medical_data` 模拟数据表（用于开发和测试）
- [X] 6.2 实现 `@MagicModule` 注解的 `BizMagicModule.java`（暴露 `deductBalance`、`getBalance`、`getQueryPrice`）
- [X] 6.3 **实现按查询类型计费**：根据请求的 `query_type` 从 `biz_query_price` 表查询单价，不同接口不同费用（如接口1=10元、接口2=11元）
- [X] 6.4 实现周期结算扣款 SQL（若依定时任务按调用目标参数统一控制结算周期，统一扣减余额，允许扣成负数作为欠费状态）
- [X] 6.5 实现查询记账 Service（`@Transactional`：查价 + 日志；不实时扣余额、不写扣费流水）
- [X] 6.6 实现 `DesensitizeUtil.java`（姓名、身份证、诊断等脱敏规则）
- [X] 6.7 编写查询接口 Magic-API 脚本（调 Java 模块完成全流程）
- [X] 6.8 **设计数据适配层接口**：定义 `MedicalDataSource` 接口（标准化查询入参/出参、数据源配置、健康检查），为 Phase 7 对接真实数据源做准备
- [X] 6.9 余额大于等于 0 时验证查询成功、余额不变、查询日志记录费用
- [X] 6.10 结算后余额小于 0 时验证返回 4001、不查询

**验证标准**：余额大于等于 0 → 查询成功、余额不实时扣减、查询日志记录费用；定时结算任务到期后统一写 `SETTLEMENT` 费用流水并扣减余额；结算后余额小于 0 → 返回 4001。数据适配层接口定义完成，可扩展对接多种数据源。

---

## Phase 7：对接真实数据源（湖南数字产业集团等）（3-4 天）

**目标**：数据适配层对接真实数据源（卫健委、卫生局、湖南数字产业集团系统），替换模拟数据，确保查询返回真实数据。

- [ ] 7.1 调研各数据源对接协议（HTTP API/SDK/数据库直连等）
- [ ] 7.2 对接湖南数字产业集团系统（主要数据源），实现 `DigitalIndustryDataSource.java` 实现 `MedicalDataSource` 接口
- [ ] 7.3 对接卫健委/卫生局数据源，实现各数据源适配器
- [X] 7.4 实现数据源路由：根据查询类型动态选择数据源
- [X] 7.5 实现数据源健康检查与熔断机制（数据源不可用时自动降级）
- [X] 7.6 配置数据源连接参数到 `application.yml`（可动态切换）
- [X] 7.7 Magic-API 查询脚本切换为调用数据适配层（去掉 mock 数据源直接依赖）
- [X] 7.8 编写数据源对接单元测试和集成测试（当前为路由/回退单元测试；真实集成测试待外部接口开通）

**验证标准**：真实数据源配置正确 → 查询返回真实数据；数据源不可用 → 降级处理有日志告警；各数据源之间可动态切换。

---

## Phase 8：管理后台查询日志与费用流水（管理员功能，1-1.5 天）

**目标**：管理员可查看/搜索/导出查询日志和费用流水。

- [X] 8.1 用代码生成器导入 `biz_query_log` 生成只读列表（删除增删改按钮）
- [X] 8.2 添加搜索条件：公司名称、查询类型、时间范围、状态
- [X] 8.3 添加日志详情查看功能
- [X] 8.4 用代码生成器导入 `biz_fee_flow` 生成只读列表（含充值记录和扣费记录）
- [X] 8.5 添加费用流水搜索和导出功能
- [X] 8.6 公司详情页关联展示查询日志和费用流水记录

**验证标准**：管理员可查看所有公司的日志和流水，只能查看不能修改，搜索和导出功能正常。

---

## Phase 9：统计分析仪表盘 + 余额查询接口 + 收尾（管理员功能，1.5-2 天）

**目标**：为管理员提供可视化统计图表供决策分析；为保险公司提供余额查询接口；生产配置就绪。

- [X] 9.1 实现统计接口（管理员可视化页面，ECharts 图表）：
  - 月度/年度查询次数分析（柱状图数据）
  - 月度/年度查询类别统计（饼图数据）
  - 近半年/近一年查询次数趋势（折线图数据）
  - 各公司查询量排名、收入汇总
- [X] 9.2 前端 ECharts 仪表盘（柱状图 + 饼图 + 折线图 + 数字卡片）
- [X] 9.3 创建签名保护余额查询接口 `GET /api/v1/balance/query`
- [ ] 9.4 E2E 全流程测试（含周期结算并发场景）
- [ ] 9.5 生产环境配置（关闭 Magic-API UI、HTTPS、Nginx）
- [X] 9.6 撰写对外接口文档（含签名计算示例）

**验证标准**：仪表盘数据正确（查询次数柱状图、类别饼图、趋势折线图），余额查询接口返回当前余额，生产配置安全。

---

## Phase 10：保险公司对接上线（15 天）

**目标**：全省 51 家保险公司逐一完成技术对接，分配 AppKey/Secret，联调测试通过。

- [X] 10.1 制定保险公司对接流程文档（对接步骤、接口说明、签名示例）
- [X] 10.2 为每家保险公司生成并发放 AppKey/Secret（批量密钥导出）
- [X] 10.3 向保险公司提供客户端签名示例（Python/Java/Shell 各一份）
- [ ] 10.4 对接测试：每家保险公司联调验证签名认证、数据查询、余额查询
- [ ] 10.5 处理对接中遇到的问题（接口适配、数据格式、异常处理）
- [X] 10.5.1 加固保险公司嵌入式医疗查询单页：同步桌面交付文件到服务器接口地址，清理复制空格/换行，识别脱敏 AppKey，区分无效 AppKey、后端未放行、网络/CORS、余额不足等错误；验证信号：桌面 HTML 脚本语法检查通过，服务器有效 AppKey 加载 18 个查询接口，医保信息查询返回 3 条记录
- [X] 10.5.2 修复保险公司端 AppKey 复制兼容性：HTTP 部署下 Clipboard API 不可用时回退到 textarea 复制，复制成功或失败均给出明确提示，并支持手动选中完整 AppKey；验证信号：RuoYi-Vue3 生产构建通过
- [X] 10.5.3 加固单页 AppKey 接入诊断：确认接入时展示已粘贴 AppKey 前后四位，认证失败时优先提示旧 Key/脱敏 Key/非当前 Key，并同步桌面交付文件；验证信号：线上当前有效 AppKey 加载 18 个查询接口，Chrome headless 打开 file:// 单页后选择“医疗大数据”提交查询，页面渲染 2 条结果记录
- [X] 10.5.4 修复单页查询成功后残留接入失败提示：查询提交前同步 AppKey 输入框，查询成功后将接入状态刷新为已接入，并移除重复嵌套的认证失败文案；验证信号：Chrome headless 模拟旧错误残留后直接查询医保信息，页面显示已接入、查询成功提示和 3 条结果记录
- [ ] 10.6 对所有保险公司进行余额初始化充值

**验证标准**：51 家保险公司接口联调通过，每家均可成功调用查询接口并正确扣费。

---

## Phase 11：用户培训（3 天）

**目标**：对中国检验湖南分公司运营人员和管理员进行系统使用培训。

- [X] 11.1 制作培训教材：
  - 快速入门（系统功能概述、基本操作指引）
  - 操作分册（不同角色的业务流程操作说明）
  - 技术资料汇编（安装配置、数据备份恢复、故障排除）
- [ ] 11.2 Level 1 培训：面向运营操作人员（开户、充值、日志查看、统计报表）
- [ ] 11.3 Level 2 培训：面向系统管理员（数据源管理、系统维护、故障处理）
- [ ] 11.4 培训考核与问题答疑
- [ ] 11.5 系统试运行与验收支持

**验证标准**：运营人员能独立完成日常操作，管理员能独立维护系统。

```
Phase1(脚手架) → Phase2(建表) → Phase3(MagicAPI) → Phase4(公司管理+公司门户)
                                                      ↓
Phase5(签名系统) → Phase6(查询+计费+适配设计) → Phase7(真实数据源对接)
                                                      ↓
                              Phase8(日志流水-管理员) → Phase9(仪表盘+收尾)
                                                               ↓
                                                    Phase10(保险公司对接)
                                                               ↓
                                                    Phase11(用户培训)
```

## 关键技术难点

| 难点                          | 方案                                                                                       |
| ----------------------------- | ------------------------------------------------------------------------------------------ |
| AppKey/Sign 与 JWT Token 共存 | `/api/v1/**` 在 SecurityConfig 中 `permitAll()`，由 `SignAuthInterceptor` 做签名认证 |
| Magic-API 调用 Java 扣费逻辑  | `@MagicModule` + `@MagicFunction` 暴露 Spring Bean                                     |
| 请求体多次读取                | `OncePerRequestFilter` + `ContentCachingRequestWrapper`                                |
| 周期结算并发控制              | 若依定时任务禁止并发执行；查询只记账不扣款，周期结算统一写 `SETTLEMENT` 流水并扣减余额 |
| 日志不可篡改                  | 数据库触发器禁止 UPDATE/DELETE + 应用层不提供修改接口                                      |
| 多数据源对接与路由            | 定义 `MedicalDataSource` 接口，策略模式动态路由，根据查询类型选择数据源                  |
| 数据源不可用降级              | 健康检查 + 熔断机制（`@CircuitBreaker`），不可用时切备用数据源或返回明确错误码           |
| 批量保险公司对接              | 自助开户页面 + 标准对接文档 + 签名示例脚本，减少一对一的沟通成本                           |

---

> 每完成一项，将 `[ ]` 改为 `[x]` 即可标记完成。

---

## 2026-05-21 费用需求调整：月度服务额度 + 月度对账

**目标**：将保险公司端弱化“收费/余额/扣费”表达，内部管理后台保留完整预算、价格和账单能力。

- [X] 设计文档：新增 `docs/月度服务额度与对账设计.md`。
- [X] 数据模型：新增公司月度预算、公司级接口价格、月度额度使用、月度账单和账单明细。
- [X] 查询链路：按公司级接口价格预留预算，按查得/未查得结果确认入账金额。
- [X] 月账单：支持按自然月生成账单，并按接口类型和结果类型汇总明细。
- [X] 管理后台：保险公司管理支持月度预算；查询价目调整为公司级接口标准；新增月度账单页面。
- [X] 保险公司端：首页、查询页、月度对账文案调整，不在主流程突出余额和本次费用，取消保险公司端额度申请入口。

**验证信号**：
- `MedicalQueryServiceImplTest` 覆盖公司级价格、查得/未查得不同价格、严格预算预留。
- `BillingSettlementServiceTest` 覆盖自然月账单按接口和结果类型汇总。
- 2026-05-21 修复公司端登录态误报：公司端 `/company/**` 使用独立布局和 `companyToken` 路由守卫，不再触发后台 Layout 的旧 token 请求；验证信号：`npm run build:prod` 通过。
- 2026-05-21 清理若依品牌露出：系统标题改为“医疗查询信息平台”，侧边栏不再显示若依 logo，公司登录页移除“管理员登录”入口，后台首页和顶部若依链接已替换/移除；验证信号：`npm run build:prod` 通过。
- 2026-05-21 修复公司端浏览器标题残留：公司端路由强制设置 `document.title` 为“医疗查询信息平台”，并替换 `vue.config.js` 与 `package.json/package-lock.json` 的若依标题兜底；验证信号：`http://localhost:8081/` HTML 标题为“医疗查询信息平台”，`npm run build:prod` 通过。
- 2026-05-21 修正公司端与价目管理：保险公司端保留“月度对账”入口并恢复 AppKey 换发；管理端查询价目改为“公司列表 -> 公司全部接口标准”维护；验证信号：`mvn -pl ruoyi-admin -am -DskipTests package` 通过，8088 后端启动成功，`npm run build:prod` 通过。
- 2026-05-21 细化月度对账和查询命中规则：月度账单生成改为按当前月、所有公司、所有接口分类生成，明细按接口展示查得/未查得次数；公司端移除额度申请入口并显示“医疗信息接口台”；mock 查询改为按接口、姓名、身份证同时匹配，不匹配时返回 `NO_RESULT`；验证信号：`mvn -pl ruoyi-admin -am -DskipTests package` 通过，`npm run build:prod` 通过，本地查询“张三/430102199001011234”返回 `NO_RESULT`。
