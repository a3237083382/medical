# 湖南省医疗信息实时查询平台 — 开发计划

> 创建日期：2026-05-09
> 预计工期：约 13 个工作日（3 周）

---

## 技术栈

| 层面 | 选型 |
|------|------|
| 后端框架 | RuoYi-Vue (master, Spring Boot 4.x + JDK 17+) |
| 前端 | Vue3 + Element Plus + Vite |
| 接口层 | Magic-API 2.3.0 (org.ssssssss) |
| 数据库 | MySQL 8.0 (utf8mb4) |
| 认证机制 | AppKey + HMAC-SHA256 签名 |
| 缓存 | Redis |

## 系统架构

```
管理后台 (RuoYi-Vue)                    对外接口 (Magic-API)
┌─────────────────────────┐           ┌──────────────────────────┐
│ 保险公司管理(开户/充值)    │           │ AppKey/Sign 认证拦截器    │
│ 查询日志查看/导出         │           │ 医疗数据查询接口          │
│ 费用流水/对账            │           │ 余额查询接口              │
│ 统计仪表盘 (ECharts)     │           │ 计费扣费引擎              │
│ RuoYi 内置功能           │           │                          │
└────────┬────────────────┘           └────────┬─────────────────┘
         │                                      │
         └──────────────┬───────────────────────┘
                        ▼
              ┌─────────────────────┐
              │  MySQL 数据库        │
              │  biz_insurance_company │
              │  biz_query_log      │
              │  biz_fee_flow       │
              │  magic_api_file     │
              │  sys_* (RuoYi 自带)  │
              └─────────────────────┘
```

---

## Phase 1：项目脚手架搭建与环境验证（1-2 天）

**目标**：RuoYi-Vue 前后端在本地成功启动，admin 登录进入后台。

- [ ] 1.1 修复本地 Java 环境（JAVA_HOME 指向 JDK 17）
- [ ] 1.2 获取 RuoYi-Vue 源码（克隆或下载官方仓库）
- [ ] 1.3 创建 MySQL 数据库 `ruoyi`，执行 RuoYi 初始化 SQL
- [ ] 1.4 配置 `application-druid.yml` 数据库连接
- [ ] 1.5 配置 `application.yml` 中的 Redis 连接
- [ ] 1.6 启动后端，验证 `http://localhost:8080` 可用
- [ ] 1.7 前端 `npm install` + `npm run dev` 启动
- [ ] 1.8 验证 `admin/admin123` 能登录后台

**验证标准**：`admin/admin123` 成功登录，菜单正常显示。

---

## Phase 2：数据库业务表设计与创建（1 天）

**目标**：三张业务表创建完成，含索引、审计字段、触发器。

### 表结构

| 表名 | 说明 | 核心字段 |
|------|------|----------|
| `biz_insurance_company` | 保险公司用户表 | id, company_name, company_code, app_key(唯一), app_secret, balance, status, contact_person, contact_phone |
| `biz_query_log` | 查询日志表 | id, company_id, query_type, query_params(JSON), fee, balance_before, balance_after, status, request_time, request_ip |
| `biz_fee_flow` | 费用流水表 | id, company_id, operation_type(RECHARGE/DEDUCTION/REFUND), amount, balance_before, balance_after, operator, biz_id, operation_time |

- [ ] 2.1 编写 `biz_insurance_company` 建表 SQL（含索引、审计字段）
- [ ] 2.2 编写 `biz_query_log` 建表 SQL（含禁止 UPDATE/DELETE 触发器）
- [ ] 2.3 编写 `biz_fee_flow` 建表 SQL（含索引）
- [ ] 2.4 合并 SQL 脚本并执行到数据库

**验证标准**：`DESC` 各表确认结构正确，触发器生效。

---

## Phase 3：Magic-API 集成（1 天）

**目标**：Magic-API 管理界面可访问，能编写并调用 SQL 接口。

- [ ] 3.1 在 `ruoyi-admin/pom.xml` 添加 `magic-api-spring-boot-starter` 依赖
- [ ] 3.2 配置 `application.yml`（database 存储模式、管理界面路径）
- [ ] 3.3 在 `SecurityConfig.java` 中放行 `/magic/web/**` 和 `/magic/api/**`
- [ ] 3.4 启动项目，进入 Magic-API Web IDE
- [ ] 3.5 创建测试接口 `GET /magic/api/test/ping`，返回 `pong`

**验证标准**：访问 `http://localhost:8080/magic/web/` 进入 IDE，调用测试接口返回成功。

---

## Phase 4：保险公司管理模块（后台 CRUD，2 天）

**目标**：管理后台支持保险公司增删改查、AppKey/Secret 自动生成、状态管理、手动充值。

- [ ] 4.1 用 RuoYi 代码生成器导入 `biz_insurance_company` 生成 CRUD
- [ ] 4.2 将生成的代码解压到对应后端和前端目录
- [ ] 4.3 实现 `AppKeyUtil.java`：生成 32 位 AppKey 和 64 位 AppSecret
- [ ] 4.4 修改 Service：新增时自动生成 AppKey/AppSecret
- [ ] 4.5 配置菜单和权限标识
- [ ] 4.6 实现启用/停用状态切换（Controller + 前端开关）
- [ ] 4.7 实现充值接口（`@Transactional` 保证余额+流水原子性）
- [ ] 4.8 前端适配：AppKey 复制按钮、余额格式化、充值对话框

**验证标准**：能创建公司（自动生成 AppKey/Secret），充值后余额增加，停用后状态标记正确。

---

## Phase 5：AppKey/Sign 签名认证系统（1.5-2 天）

**目标**：对外 API 路径经过签名认证，合法请求放行，非法请求拦截。

### 签名协议

```
请求头: X-App-Key, X-Timestamp, X-Nonce, X-Sign
签名算法: HMAC-SHA256(timestamp + "\n" + nonce + "\n" + body, appSecret)
时效窗口: ±5 分钟
Nonce 去重: Redis SET + EXPIRE (TTL = 6 分钟)
```

- [ ] 5.1 实现 `SignUtil.java`（HMAC-SHA256 签名生成和验证）
- [ ] 5.2 实现 `SignAuthInterceptor.java`（AppKey 查公司 → 校验状态 → 验证签名 → Nonce 去重）
- [ ] 5.3 在 `WebMvcConfig.java` 注册拦截器到 `/api/v1/**`
- [ ] 5.4 在 `SecurityConfig.java` 放行 `/api/v1/**`
- [ ] 5.5 实现请求体缓存 Filter（`ContentCachingRequestWrapper`）
- [ ] 5.6 创建受保护测试接口，用 curl 验证签名流程
- [ ] 5.7 编写客户端签名示例脚本（Python 或 Shell）

**验证标准**：无签名头 → 401，签名错误 → 401，Nonce 重用 → 401，超时 → 401，正确签名 → 200。

---

## Phase 6：医疗数据查询 API + 计费引擎（核心业务，2-3 天）

**目标**：完整的"查询 → 检查余额 → 扣费 → 脱敏 → 日志"链路跑通。

- [ ] 6.1 设计并创建 `mock_medical_data` 模拟数据表
- [ ] 6.2 实现 `@MagicModule` 注解的 `BizMagicModule.java`（暴露 `deductBalance`、`getBalance`）
- [ ] 6.3 实现原子扣款 SQL（`UPDATE balance = balance - ? WHERE balance >= ?`）
- [ ] 6.4 实现扣费 Service（`@Transactional`：扣费 + 日志 + 流水）
- [ ] 6.5 实现 `DesensitizeUtil.java`（姓名、身份证、诊断等脱敏规则）
- [ ] 6.6 编写查询接口 Magic-API 脚本（调 Java 模块完成全流程）
- [ ] 6.7 余额充足时验证查询成功、余额扣减、日志和流水记录
- [ ] 6.8 余额不足时验证返回 4001、不查询、不扣费

**验证标准**：余额充足 → 查询成功、余额扣减、有日志流水；余额不足 → 返回 4001。

---

## Phase 7：管理后台日志与流水管理（1-1.5 天）

**目标**：运营人员可查看查询日志、费用流水、导出报表。

- [ ] 7.1 用代码生成器导入 `biz_query_log` 生成只读列表（删除增删改按钮）
- [ ] 7.2 添加搜索条件：公司名称、查询类型、时间范围、状态
- [ ] 7.3 添加日志详情查看功能
- [ ] 7.4 用代码生成器导入 `biz_fee_flow` 生成只读列表
- [ ] 7.5 添加费用流水搜索和导出功能
- [ ] 7.6 公司详情页关联展示充值记录

**验证标准**：日志和流水页面只能查看，搜索和导出功能正常。

---

## Phase 8：统计图表、余额查询接口、收尾（1.5-2 天）

**目标**：仪表盘就绪、保险公司可自查余额、生产配置就绪。

- [ ] 8.1 实现统计接口：日查询量、公司排名、收入汇总
- [ ] 8.2 前端 ECharts 仪表盘（折线图 + 饼图 + 柱状图 + 数字卡片）
- [ ] 8.3 创建 Magic-API 余额查询接口 `GET /api/v1/balance/query`
- [ ] 8.4 E2E 全流程测试（含并发扣款场景）
- [ ] 8.5 生产环境配置（关闭 Magic-API UI、HTTPS、Nginx）
- [ ] 8.6 撰写对外接口文档（含签名计算示例）

**验证标准**：仪表盘数据正确，余额查询接口返回当前余额，生产配置安全。

---

## 依赖关系

```
Phase1(脚手架) → Phase2(建表) → Phase3(MagicAPI) → Phase4(公司管理)
                                                      ↓
Phase5(签名系统) → Phase6(查询+计费) → Phase7(日志流水) → Phase8(仪表盘+收尾)
```

## 关键技术难点

| 难点 | 方案 |
|------|------|
| AppKey/Sign 与 JWT Token 共存 | `/api/v1/**` 在 SecurityConfig 中 `permitAll()`，由 `SignAuthInterceptor` 做签名认证 |
| Magic-API 调用 Java 扣费逻辑 | `@MagicModule` + `@MagicFunction` 暴露 Spring Bean |
| 请求体多次读取 | `OncePerRequestFilter` + `ContentCachingRequestWrapper` |
| 并发扣款 | 原子 SQL `UPDATE balance = balance - ? WHERE balance >= ?` |
| 日志不可篡改 | 数据库触发器禁止 UPDATE/DELETE + 应用层不提供修改接口 |

---

> 每完成一项，将 `[ ]` 改为 `[x]` 即可标记完成。
