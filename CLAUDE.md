# 湖南省医疗信息实时查询平台

## 项目定位

保险公司在承保前查询投保人医疗数据，用于风险评估。平台作为中间人，对接卫健委/医院数据源，对外暴露查询接口给全省51家保险公司，按查询次数收费。

## 业务模式

B2B API 服务，非面向消费者。
- 保险公司签约后，由运营方在后台手动开户，分配 AppKey/AppSecret
- 保险公司先充值（公对公转账），运营方后台手动上账
- 每次调接口查询，实时扣费，余额不足拒绝查询
- 充值记录和扣费明细存费用流水表

## 技术栈

- **开发框架**：诺依框架（RuoYi），基于 Spring Boot + Vue，前后端分离
- **接口层**：MagicAPI，用于将 SQL 快速包装成 RESTful JSON 接口，暴露给保险公司调用
- **数据库**：MySQL（诺依默认）

## 系统分工

### 管理后台（诺依框架）— 内部人员使用

- 管理保险公司账号（开户/停用/充值）
- 查看查询日志和统计图表
- 诺依已内置：用户管理、角色权限、日志管理、菜单管理

### 对外接口（MagicAPI）— 保险公司调用

- 提供 JSON 接口供保险公司查询医疗数据
- 请求时校验 AppKey/AppSecret 签名
- 查询前检查账户余额，查询后记录日志并扣费

## 数据库核心表

| 表 | 说明 |
|----|------|
| 保险公司用户表 | 公司名、AppKey、AppSecret、余额、状态 |
| 查询日志表 | 保险公司ID、查询时间、查询类型、查询参数、扣费金额 |
| 费用流水表 | 保险公司ID、操作类型（充值/扣费/退款）、金额、操作前后余额、时间 |

## 规则

### 开发规则

1. **先设计，后编码** — 任何功能开发前先产出设计文档，再出实现计划，最后编码
2. **一次只做一件事** — 不并行开发多个功能，每个功能完成后再推进下一个
3. **代码简洁优先** — 不添加需求之外的功能，不为一次性场景做抽象，不写多余的注释
4. **精准修改** — 只改动必须改动的代码，不顺手"优化"无关代码
5. **目标驱动** — 每个任务定义明确的可验证成功标准
6. **外部数据脱敏** — 返回给保险公司的数据必须经过脱敏处理
7. **查询留痕** — 每一次查询必须记录日志，不可抵赖，用于计费

### 安全规则

1. 接口鉴权使用 AppKey + Sign 签名机制，不传输明文密码
2. 费用相关的数据库操作需要事务保证，避免多扣或少扣
3. 用户余额操作需加锁或乐观锁防止并发扣款问题
4. 不将内部数据库结构直接暴露给外部接口

### 计费规则

1. 先充值，后使用 — 保险公司需预存费用
2. 每次查询实时扣费，余额不足时返回错误码，不查询
3. 查询日志即计费凭证，不允许修改或删除
4. 费用流水不可逆删除，仅允许冲正操作

## 后续要做的事

- [ ] 产出详细设计文档
- [ ] 搭建诺依框架基础工程
- [ ] 集成 MagicAPI
- [ ] 设计并创建数据库表
- [ ] 用户认证模块（AppKey/Secret 签发与校验）
- [ ] 医疗数据查询模块（对接外部数据源）
- [ ] 数据脱敏模块
- [ ] 计费扣费模块（查询时扣费）
- [ ] 费用充值管理（后台手动上账）
- [ ] 查询日志与统计图表
- [ ] 余额查询接口（供保险公司查看）
- [ ] 接口文档撰写
- [ ] 对接 51 家保险公司
