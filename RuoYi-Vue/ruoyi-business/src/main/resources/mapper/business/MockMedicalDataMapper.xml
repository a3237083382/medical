<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.business.mapper.MockMedicalDataMapper">

    <resultMap type="com.ruoyi.business.domain.MockMedicalData" id="MockMedicalDataResult">
        <result property="id" column="id"/>
        <result property="queryType" column="query_type"/>
        <result property="patientName" column="patient_name"/>
        <result property="idCard" column="id_card"/>
        <result property="diagnosis" column="diagnosis"/>
        <result property="dataJson" column="data_json"/>
        <result property="status" column="status"/>
        <result property="remark" column="remark"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <select id="selectAvailableByQueryType" parameterType="String" resultMap="MockMedicalDataResult">
        select id, query_type, patient_name, id_card, diagnosis, data_json, status, remark, create_time
        from mock_medical_data
        where query_type = #{queryType}
          and status = '0'
        order by id asc
        limit 1
    </select>

    <select id="selectAvailableByQuery" resultMap="MockMedicalDataResult">
        select id, query_type, patient_name, id_card, diagnosis, data_json, status, remark, create_time
        from mock_medical_data
        where query_type = #{queryType}
          and patient_name = #{patientName}
          and id_card = #{idCard}
          and status = '0'
        order by id asc
        limit 1
    </select>

    <select id="selectAvailableByIdCard" resultMap="MockMedicalDataResult">
        select id, query_type, patient_name, id_card, diagnosis, data_json, status, remark, create_time
        from mock_medical_data
        where query_type = #{queryType}
          and id_card = #{idCard}
          and status = '0'
        order by id asc
        limit 1
    </select>
</mapper>
