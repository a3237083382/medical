<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.business.mapper.BizFeeFlowMapper">

    <resultMap type="com.ruoyi.business.domain.BizFeeFlow" id="BizFeeFlowResult">
        <result property="id" column="id"/>
        <result property="companyId" column="company_id"/>
        <result property="companyName" column="company_name"/>
        <result property="operationType" column="operation_type"/>
        <result property="amount" column="amount"/>
        <result property="balanceBefore" column="balance_before"/>
        <result property="balanceAfter" column="balance_after"/>
        <result property="operator" column="operator"/>
        <result property="bizId" column="biz_id"/>
        <result property="operationTime" column="operation_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <sql id="selectVo">
        select f.id, f.company_id, c.company_name, f.operation_type, f.amount,
               f.balance_before, f.balance_after, f.operator, f.biz_id,
               f.operation_time, f.remark
        from biz_fee_flow f
        left join biz_insurance_company c on f.company_id = c.id
    </sql>

    <select id="selectBizFeeFlowById" parameterType="Long" resultMap="BizFeeFlowResult">
        <include refid="selectVo"/>
        where f.id = #{id}
    </select>

    <select id="selectBizFeeFlowList" parameterType="com.ruoyi.business.domain.BizFeeFlow" resultMap="BizFeeFlowResult">
        <include refid="selectVo"/>
        <where>
            <if test="companyId != null">and f.company_id = #{companyId}</if>
            <if test="companyName != null and companyName != ''">and c.company_name like concat('%', #{companyName}, '%')</if>
            <if test="operationType != null and operationType != ''">and f.operation_type = #{operationType}</if>
            <if test="params.beginTime != null and params.beginTime != ''">and f.operation_time &gt;= #{params.beginTime}</if>
            <if test="params.endTime != null and params.endTime != ''">and f.operation_time &lt;= #{params.endTime}</if>
        </where>
        order by f.operation_time desc
    </select>

    <insert id="insertBizFeeFlow" parameterType="com.ruoyi.business.domain.BizFeeFlow" useGeneratedKeys="true" keyProperty="id">
        insert into biz_fee_flow (
            company_id, operation_type, amount, balance_before, balance_after,
            operator, biz_id, operation_time, remark
        ) values (
            #{companyId}, #{operationType}, #{amount}, #{balanceBefore}, #{balanceAfter},
            #{operator}, #{bizId}, sysdate(), #{remark}
        )
    </insert>

</mapper>
