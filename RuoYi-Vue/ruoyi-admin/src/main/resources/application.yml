# 项目相关配置
ruoyi:
  # 名称
  name: RuoYi
  # 版本
  version: 3.9.2
  # 版权年份
  copyrightYear: 2026
  # 文件路径 示例（ Windows配置D:/ruoyi/uploadPath，Linux配置 /home/ruoyi/uploadPath）
  profile: D:/ruoyi/uploadPath
  # 获取ip地址开关
  addressEnabled: false
  # 验证码类型 math 数字计算 char 字符验证
  captchaType: math

# 开发环境配置
server:
  # 服务器的HTTP端口，默认为8080
  port: 8088
  servlet:
    # 应用的访问路径
    context-path: /
  tomcat:
    # tomcat的URI编码
    uri-encoding: UTF-8
    # 连接数满后的排队数，默认为100
    accept-count: 1000
    threads:
      # tomcat最大线程数，默认为200
      max: 800
      # Tomcat启动初始化的线程数，默认值10
      min-spare: 100

# 日志配置
logging:
  level:
    com.ruoyi: debug
    org.springframework: warn

# 用户配置
user:
  password:
    # 密码最大错误次数
    maxRetryCount: 5
    # 密码锁定时间（默认10分钟）
    lockTime: 10

# Spring配置
spring:
  # 资源信息
  messages:
    # 国际化资源文件路径
    basename: i18n/messages
  profiles:
    active: druid
  # 文件上传
  servlet:
    multipart:
      # 单个文件大小
      max-file-size: 10MB
      # 设置总上传的文件大小
      max-request-size: 20MB
  # 服务模块
  devtools:
    restart:
      # 热部署开关
      enabled: true
  data:
    # redis 配置
    redis:
      # 地址
      host: localhost
      # 端口，默认为6379
      port: 6379
      # 数据库索引
      database: 0
      # 密码
      password:
      # 连接超时时间
      timeout: 10s
      lettuce:
        pool:
          # 连接池中的最小空闲连接
          min-idle: 0
          # 连接池中的最大空闲连接
          max-idle: 8
          # 连接池的最大数据库连接数
          max-active: 8
          # #连接池最大阻塞等待时间（使用负值表示没有限制）
          max-wait: -1ms

# token配置
token:
  # 令牌自定义标识
  header: Authorization
  # 令牌密钥
  secret: abcdefghijklmnopqrstuvwxyz
  # 令牌有效期（默认30分钟）
  expireTime: 30

# MyBatis配置
mybatis:
  # 搜索指定包别名
  typeAliasesPackage: com.ruoyi.**.domain
  # 配置mapper的扫描，找到所有的mapper.xml映射文件
  mapperLocations: classpath*:mapper/**/*Mapper.xml
  # 加载全局的配置文件
  configLocation: classpath:mybatis/mybatis-config.xml

# PageHelper分页插件
pagehelper:
  helperDialect: mysql
  supportMethodsArguments: true
  params: count=countSql

# Springdoc配置
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
    tags-sorter: alpha
  group-configs:
    - group: 'default'
      display-name: '测试模块'
      paths-to-match: '/**'
      packages-to-scan: com.ruoyi.web.controller.tool

# 防盗链配置
referer:
  # 防盗链开关
  enabled: false
  # 允许的域名列表
  allowed-domains: localhost,127.0.0.1,ruoyi.vip,www.ruoyi.vip

# 防止XSS攻击
xss:
  # 过滤开关
  enabled: true
  # 排除链接（多个用逗号分隔）
  excludes: /system/notice
  # 匹配链接
  urlPatterns: /system/*,/monitor/*,/tool/*

# Magic-API 配置
magic-api:
  # Web IDE 路径
  web: /magic/web
  # API 前缀
  prefix: /magic/api
  # 资源存储配置（数据库模式）
  resource:
    datasource: ""
    type: database
    table-name: magic_api_file
  # 启用Swagger
  swagger:
    enabled: true
  # 打印banner
  banner: true

# Medical data source routing. Real source credentials are filled after external systems open.
medical:
  datasource:
    default-source: mock
    fallback-enabled: false
    routes:
      medical_all: mock
    digital-industry:
      # 测试阶段配置，接入真实环境前请替换为数产提供的正式配置。
      enabled: "${DIGITAL_INDUSTRY_ENABLED:true}"
      query-url: "${DIGITAL_INDUSTRY_QUERY_URL:http://openapi-test.datafty.hunan-data.com/ecsb/gw}"
      timeout-millis: 5000
      sdk-classpath: "${DIGITAL_INDUSTRY_SDK_CLASSPATH:C:/Users/mec/Desktop/java-client-demo825}"
      environment: "${DIGITAL_INDUSTRY_ENVIRONMENT:test}"
      partner-id: "${DIGITAL_INDUSTRY_PARTNER_ID:08JG300000}"
      sys-id: "${DIGITAL_INDUSTRY_SYS_ID:08JG300004}"
      app-sub-id: "${DIGITAL_INDUSTRY_APP_SUB_ID:08JG3000040001}"
      app-token: "${DIGITAL_INDUSTRY_APP_TOKEN:01d23da203d74ae79de3aa33168f86d5}"
      msg-secret: "${DIGITAL_INDUSTRY_MSG_SECRET:0b0e5c5b27654694aaca6944}"
      sign-secret: "${DIGITAL_INDUSTRY_SIGN_SECRET:22e1cebdc66445e9b3ce693943e69536}"
      data-service-id: "${DIGITAL_INDUSTRY_DATA_SERVICE_ID:ds2092069996838387712}"
      access-key-id: "${DIGITAL_INDUSTRY_ACCESS_KEY_ID:2297a34f14f8687c}"
      access-key-secret: "${DIGITAL_INDUSTRY_ACCESS_KEY_SECRET:0c6a831eb47291163118899c1fbdd68e}"
      sm2-public-key: "${DIGITAL_INDUSTRY_SM2_PUBLIC_KEY:MFkwEwYHKoZIzj0CAQYIKoEcz1UBgi0DQgAEXeLG0wlPuqO+YtY5ECdMTw9ZlFJzhaLaU9Xqs/lvz0ILGSkrQJULu5zzdaPo0GBf19A0u2igkWPqEqqOvO9kUQ==}"
      api-id: "${DIGITAL_INDUSTRY_API_ID:58b47a10fb32478a.5de6aecd87184e42.1605235db8fb4c1f.2092071974363545601:1787623860821}"

# 保险公司独立嵌入页面跨域白名单。生产环境使用逗号分隔的实际部署来源覆盖此环境变量。
embedded:
  cors:
    allowed-origin-patterns: "${EMBEDDED_CORS_ALLOWED_ORIGIN_PATTERNS:http://localhost:[*],http://127.0.0.1:[*]}"
